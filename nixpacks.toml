[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = [
  "cd api && pip install -r requirements.txt",
  "cd api && playwright install --with-deps chromium"
]

[phases.build]
cmds = []

[start]
cmd = "cd api && uvicorn main:app --host 0.0.0.0 --port $PORT"
