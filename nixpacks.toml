[phases.setup]
nixPkgs = ["python311", "pipenv"]

[phases.install]
cmds = ["cd api && pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "cd api && uvicorn main:app --host 0.0.0.0 --port $PORT"
